---
const { profile } = Astro.props;
---

<section class="card" aria-labelledby="summary-title">
	<h2 id="summary-title">{profile.title}</h2>
	{profile.paragraphs?.map((paragraph) => (
		<p>{paragraph}</p>
	))}
</section>
